const mongoose = require('mongoose');

// entries are the diary entries which will be the backbone of the site
// they will be part of the game model

const entrySchema = new mongoose.Schema({
  content: []
})


const entryStatisticsSchema = new mongoose.Schema({
  
})


const entryContentSchema = new mongoose.Schema({
  
})
