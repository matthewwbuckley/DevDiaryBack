const mongoose = require('mongoose');

const userSchema = new mongoose.Schema({

})


// user statistics
const userStatisticsSchema = new mongoose.Schema({

})

// user settings
const userSettingsSchema = new mongoose.Schema({

})

// notifications
const userNotificationSchema = new mongoose.Schema({
  
})

// notifications for messages that have been sent to the user
const userMessageSchema = new mongoose.Schema({
  
})

// links such as following, followed by etc.
const userSocialSchema = new mongoose.Schema({
  
})

// groups that the user can be in
const userGroupSchema = new mongoose.Schema({
  
})

// games that the user can be in
const userGameSchema = new mongoose.Schema({
  
})
