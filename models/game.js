const mongoose = require('mongoose');

const gameSchema = new mongoose.Schema({
  
})

// the entry for the game
const gameEntrySchema = new mongoose.Schema({
  
})

// users and their powers on the game page
const gameUserSchema = new mongoose.Schema({
  
})


// links to other media
const gameLinkSchema = new mongoose.Schema({
  
})


// the entries in the game diary
// this has been moved to the entry file.
const gameEntrySchema = new mongoose.Schema({
  
})